
 const add = (x,y) => x + y;
 const multiply = (x,y) => (x*y);

 const getName = () => {
    return 'Jim';
  };

exports.add = add;
exports.multiply = multiply;
exports.getName = getName;
